import { lightColors, darkColors } from './colors';

export const themeConfig = {
  light: {
    colors: lightColors,
    radius: '0.5rem',
  },
  dark: {
    colors: darkColors,
    radius: '0.5rem',
  },
};